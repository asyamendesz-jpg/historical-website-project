/**
 * Публичная конфигурация формы (без секретов).
 * contactEmail — получатель заявок Portfo (mailto fallback и подсказки в UI).
 * apiUrl — URL serverless API после деплоя на Vercel.
 */
window.PORTFO_FORM = {
  contactEmail: "work@asyamendes.ru",
  apiUrl: "",
};
